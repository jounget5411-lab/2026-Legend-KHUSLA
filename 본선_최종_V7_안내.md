# 본선 최종 V7 코드 안내

## 원본과 보관 방식

- 팀 저장소: `KHUSLA-SANDI/KOOKMIN`
- 원본 기준: `final/v7-current` / `final-v7-20260824`
- 고정 커밋: `467f6dda6b71c48567225d3260d925d72818e3c1`
- 개인 저장소에 반영한 날짜: 2026-09-10

`본선/`은 위 커밋의 전체 파일 트리다. 원본의 `src/`, `reports/`, `.gitignore`를 모두 포함한다.
원본 소스, 설정, 파일 모드를 바꾸지 않았으며 현재 차량을 원격으로 수정하거나 실행한 작업은 없다.
기존 개인 저장소의 `본선/track_drive`는 `보관/본선_v3.1/track_drive`로 보존했다.

## 주요 코드

| 경로 (`본선/` 기준) | 역할 |
|---|---|
| `src/track_drive_cnn_gpt/track_drive_cnn_gpt/yolo_bev_node.py` | 카메라에서 차선을 검출하고 BEV 입력 생성 |
| `src/track_drive_cnn_gpt/track_drive_cnn_gpt/cnn_path_node.py` | LiDAR 결합, 모드 선택, CNN 경로 생성 |
| `src/track_drive_cnn_gpt/track_drive_cnn_gpt/simple_motion_node.py` | 경로 추종 및 조향·속도 계산 |
| `src/track_drive_cnn_gpt/config/perception_cnn.yaml` | 모델 경로·체크섬, 신호·모드·LiDAR 설정 |
| `src/track_drive_cnn_gpt/config/simple_motion.yaml` | 최종 경로 추종 설정 |
| `src/track_drive_cnn_gpt/launch/drive.launch.py` | 최종 대회 주행 실행 진입점 |

일반·지름길·추월·라바콘 모델을 로드하고 현재 모드에 해당하는 모델을 추론한다.
최종 V7의 추월은 `hardcoded_all` 전략이 우선한다. 이름에 `low_speed`가 남은 내부 launch도 최종 코드의 일부다.
`reports/`에 남은 V2/V3 문서는 개발 이력이며 최종 설정은 V7 소스와 YAML을 기준으로 확인한다.

## 모델 및 차량 외부 설정

Git 원본 코드가 참조한 모델은 차량의 `/home/xytron/xycar_ws/models/`에 별도로 배치되어 있었다.
이번 보관에서 확보·검증한 파일은 [본선_배포모델](본선_배포모델/)에 포함했다. 각 파일의 검증 범위는 `manifest.json`에 기록했다.
이름이 같은 과거 학습 결과 대신 최종 설정과 대조한 파일을 사용해야 한다.

다음 항목은 코드만 내려받는 것으로 복원되지 않는다.

- Ubuntu / ROS2 Humble 및 관련 Python·OpenVINO·PyTorch 의존성
- ROS1 모터 브리지와 차량의 nonblocking `xycar_motor.py` 변경
- 차량의 `steering_calibration.yaml` 조향 보정 (`angle_offset: 1.952`)
- `/home/xytron/motor_up.sh`의 demand-driven bridge 구성
- 카메라 노출·게인, LiDAR/카메라 장치 연결, VESC 및 센서 드라이버 환경

## 원래 차량 환경에서의 빌드와 실행

아래는 모델·브리지·센서 설정이 완료된 원래 차량 환경을 전제로 한 명령이다.
`drive.launch.py`는 실제 주행 출력을 활성화하므로 코드 열람이나 단순 PC 검증용 명령으로 실행하지 않는다.

```bash
cd 본선
source /opt/ros/humble/setup.bash
colcon build --symlink-install
source install/setup.bash
ros2 launch track_drive_cnn_gpt drive.launch.py
```

뷰어를 포함하는 진입점은 `ros2 launch track_drive_cnn_gpt drive_viewer.launch.py`다.
Windows에서 수행한 이번 작업은 파일 동일성·모델 체크섬 검증이며, ROS 빌드나 실차 재주행 검증은 아니다.
